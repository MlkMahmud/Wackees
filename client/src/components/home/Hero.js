import React from 'react';
import SearchBar from './SearchBar';

const Hero = () => (
  <section className="hero">
    <div className="hero_search_bar_container">
      <h2>Start the day with your favorite breakfast</h2>
      <SearchBar />
    </div>
  </section>
);

export default Hero;
